{
  "name": "netintel",
  "version": "1.5.0",
  "private": true,
  "description": "Self-hosted DNS observability and network intelligence platform built on Technitium DNS Server.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:server": "npm run dev -w @netintel/server",
    "dev:web": "npm run dev -w @netintel/web",
    "build": "npm run build -w @netintel/shared && npm run build -w @netintel/server && npm run build -w @netintel/cli && npm run build -w @netintel/web",
    "db:generate": "npm run db:generate -w @netintel/server",
    "db:migrate": "npm run db:migrate -w @netintel/server",
    "cli": "npm run start -w @netintel/cli --",
    "test": "vitest run",
    "test:watch": "vitest",
    "docs:metrics": "npm run docs:metrics -w @netintel/shared"
  },
  "engines": {
    "node": ">=26"
  },
  "os": [
    "win32",
    "linux"
  ],
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
