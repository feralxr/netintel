# Copy to .env in this same directory (packages/cli/.env), or in whatever
# directory you run `netintel` from — dotenv loads .env from the current
# working directory.

# Which netintel API server to talk to. Defaults to http://localhost:8787 if unset.
NETINTEL_API_URL=http://localhost:8787
